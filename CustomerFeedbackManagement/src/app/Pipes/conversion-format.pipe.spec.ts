import { ConversionFormatPipe } from './conversion-format.pipe';

describe('ConversionFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ConversionFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
